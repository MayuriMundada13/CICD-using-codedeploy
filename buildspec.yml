version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
    commands:
      - mvn clean install -DskipTests
  build:
    commands:
      - mvn package -DskipTests
artifacts:
  files:
    - target/*.jar
    - scripts/*.sh
    - appspec.yml
  discard-paths: yes